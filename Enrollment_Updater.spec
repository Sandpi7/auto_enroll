# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['Y:\\auto_enroll\\enrollment_updater.py'],
    pathex=[],
    binaries=[],
    datas=[('LICENSE', '.'), ('README.md', '.'), ('Y:\\auto_enroll\\autoenroll', 'autoenroll')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='Enrollment_Updater',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=False,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    uac_admin=True,
)
